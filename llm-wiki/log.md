# godot-m — Log

이 프로젝트의 시간순 기억. 최신이 위로.

> **형식 계약 (훅이 파싱한다)**: 날짜 섹션은 `## YYYY-MM-DD`, 항목은 `- **제목**: 내용`.
> 코드 repo의 SessionStart 훅이 최신 섹션의 **제목**들만 추출해 세션에 주입한다.
> 제목은 주입만 보고도 무슨 일이 있었는지 알 수 있게 쓴다.

## 2026-08-16
- **밀린 검증 착수 — 2건 통과, 3건은 addon 버전 스큐로 차단**: 에디터를 켜고 godot-mcp로 2026-08-02 커밋분 검증을 시도했다. **통과**: (1) 에디터가 열리면서 `assets/characters/{hyunwoo,sua}/`의 `.import` 27개가 전부 생성됨 — 이번 커밋에 포함(이걸 빼면 export 빌드·타 머신에서 재발). (2) 게임 부팅·스크립트 파싱 정상 — 에디터 로그에 Parse Error·SCRIPT ERROR **0건**, `is_playing=true`, 게임 브리지가 `godot_input get_map`에 `source: game`으로 응답. 즉 `story_manager.gd`·`choice_controller.gd` 수정은 문법·로드 레벨에서는 문제없다. **차단**: 선택지 `condition` 동작, 스탠딩 일러스트 실제 표시, 이름표 "동현"·엑스트라 6종은 관측 채널이 막혀 확인 불가.
- **환경 블로커 — godot-mcp addon 버전 스큐 (MCP 서버 4.1.0 vs 프로젝트 addon 2.15.0)**: `godot_runtime_state`·`godot_exec`이 `UNKNOWN_COMMAND`로 거부되고 `screenshot_game`은 640px·512px 모두 `TIMEOUT`. 관측 수단이 전부 막혀 **런타임 값 확인도 외관 확인도 불가능**하고, 살아 있는 것은 입력 주입과 에디터 로그뿐이다. CLAUDE.md 분류상 환경 문제라 코드로 우회하지 않았다. 해소 절차: Godot 종료 후 `npx @satelliteoflove/godot-mcp --install-addon "<프로젝트 경로>"` 실행 → 에디터 재기동. **이게 풀리기 전에는 이 저장소의 공식 검증 입구가 사실상 닫혀 있다.**
- **과제 6은 addon을 고쳐도 그대로는 검증 불가 — 선행 준비물 발견**: `condition` 필드를 쓰는 스토리 노드가 **프로젝트 전체에 0건**이라(전 노드 스캔 확인) 게임을 돌려도 검증 대상 자체가 없다. `story/test/hub_condition_test.json`에 조건 없음/참/거짓/**미정의 변수** 4항목을 한 화면에 띄우는 노드를 만들고 `TestMenu2`에 진입 항목을 추가해야 한다. 이러면 Phase 0의 `test` 디렉터리 로드와 "미정의 변수는 0으로 조용히 평가" 함정까지 한 번에 검증된다. 사용자 승인 대기 중(스토리 파일 수정 필요).
- **정정 2건**: (1) 2026-08-15 기록의 "`Opening` 노드에 도달할 경로가 없음"은 **오류** — 실제로는 `Start`→`TestMenu`→테스트 1~4 중 하나를 완주→`TestMenu2`→8번 "전체 통합 테스트"→`Opening`으로 도달한다. 과제 3의 성격은 "도달 불가"가 아니라 "실 시나리오가 테스트 시퀀스 완주 뒤에 묻혀 있음"이며, 해당 항목은 수정해 두었다. (2) `vn_skip`의 실제 바인딩은 **Tab**인데 README는 "Ctrl로 스킵"이라고 서술 — README 부정확 항목에 추가.

## 2026-08-15
- **전체 프로젝트 분석 — 에셋 참조 17건 파손·임포트 부채 발견 (코드 변경 없음)**: 읽기 전용 점검. **`story_manager.gd`의 `scene_map` 30개 중 17개가 실존하지 않는 파일을 가리킨다** — CG 13종 전량(`first_lunch`~`ending_c`)과 배경 10종(`cafeteria_day`·`lunch_spot`·`school_grounds_*`·`city_*`·`jeju_*`·`sports_festival`·`school_front_day/evening`). `gallery_screen.gd:12`의 `GALLERY_FILES` 13개도 전부 부재라 해금돼도 빈 프레임이 뜬다. **2026-08-02 기록의 "gallery_screen.gd가 디렉터리를 스캔하는 구조"는 사실이 아니다** — 하드코딩 딕셔너리이며, 현재 `assets/gallery/`의 24장(hana/sora/yuu 계열)은 코드가 단 한 장도 참조하지 않는 타 프로젝트 잔재다(삭제 보류 근거가 무효). 반면 `opening.json`이 실제 참조하는 배경·사운드·표정·jump target은 전부 정합(dangling 0건). 또 **`assets/characters/{hyunwoo,sua}/`의 webp 27장에 `.import` 파일이 없다** — 같은 폴더 `unknown/`은 커밋돼 있어 불일치이고, 스탠딩 일러스트가 아직 한 번도 임포트·표시 검증되지 않았다는 뜻(export 빌드 위험). `unknown/`에 `.enc` 잔재 6개. README는 구프로젝트 기준(BGM 5곡·CG 24장·hana/haru/sora)이라 실물과 어긋난다.
- **과제 1 착수 전에 넣어야 할 엔진 방어 2건 식별**: (1) `story_manager.gd:162`가 `_labels[label] = data[label]`로 전역 평면 병합이라 **JSON 파일이 19개로 늘면 라벨 이름 충돌 시 경고 없이 덮어쓴다** — 로드 시 중복 검사 필요. (2) `evaluate_condition`이 미정의 변수를 `null→0`으로 조용히 처리해 **`condition` 오타가 "선택지 실종"으로만 나타난다**(H-1 디버깅 난이도) — `get_var` null 시 경고 필요. 부수 발견: `advance()`가 `_labels[current_label]`을 존재 확인 없이 인덱싱해(`:188`) 사라진 라벨의 세이브 로드 시 크래시, `Start`가 `TestMenu`로 점프해 **실 시나리오가 테스트 메뉴 뒤에 묻혀 있음** — 도달 경로는 존재하나(`Start`→`TestMenu`→테스트 1~4 중 하나를 끝까지 완주→`TestMenu2`→8번 "전체 통합 테스트"→`Opening`) 새 게임에서 프롤로그를 보려면 테스트 시퀀스를 완주해야 한다(과제 3 근거 보강), `title_screen.gd:64`의 private 메서드 외부 호출, `choice_controller.gd:43`의 `free()`.
- **과제 2(Supabase)의 실제 범위가 기록보다 넓음**: `_supabase_url`이 빈 문자열인 것에 더해 **`_display_stats_preview`(`choice_controller.gd:122`)와 `_show_stats_result`(`:158`)의 표시 로직 본문이 비어 있다** — URL을 주입해도 화면에는 아무것도 표시되지 않고 버튼만 disabled된 채 2.5초 대기한다. 과제 2 완료 기준의 "통계 미리보기 표시"는 UI 구현을 포함해야 한다.

## 2026-08-02
- **미사용 타 프로젝트 잔재 에셋 정리 (약 10MB)**: 코드·씬·JSON 전체에서 참조 0곳임을 확인하고 삭제 — `music/{hana-ending,sora-ending,harem-ending}.mp3`(8.2MB), `characters/{haru,sora,hana}/`(1.4MB). `audio_manager.gd`는 `MUSIC_PATH + id + ".mp3"` 경로 조합 방식이라 하드코딩 목록이 없어 파일 삭제만으로 안전하다. 실사용 음악은 `acoustic-chill`·`sunny-day` 둘뿐. `characters/unknown/`은 동현이 쓰고 있어 유지. `gallery/`(3.5MB)도 타 프로젝트 CG로 보이나 `gallery_screen.gd`가 디렉터리를 스캔하는 구조라 삭제 시 갤러리 화면이 비어 보류.
- **현우·수아 실제 스탠딩 일러스트 도입 — 임시 에셋 교체**: 원본 27종(현우 6·수아 21, 2192×7278 등 총 90MB)을 높이 1200px로 리사이즈해 `assets/characters/{hyunwoo,sua}/`에 배치(**90MB → 7.7MB**). 표시 슬롯이 높이 900px·비율 유지(`main_scene.tscn` `stretch_mode=5`)라 1200px면 충분하다. 원본 폴더에는 `.gdignore`를 두어 Godot 임포트에서 제외. 수아 파일명이 얼굴 이모티콘 인코딩(`a`=뜬 눈, `caret2`=^^, `lparen`=`(` 시무룩, `rparen`=`)` 미소, `O_O`=벌린 입, `semi`=땀)임을 해독하고 실물 확인 후 매핑 — `normal`은 무표정 `sua_f`, `happy`는 `sua_a_caret2_D`, `shy`는 `sua_a_caret2_rparen`. 현우 6종 전량·수아 11종 사용, 매핑 22건 파일 실존 검증 통과. `sips`는 webp 쓰기를 지원하지 않아 PNG로 유지(기존 에셋은 webp). **동현 일러스트는 없어 임시 에셋 유지**(O-7). 원본 90MB의 git 커밋 여부는 미결(O-10).
- **허브 엔진 정비 (Phase 0 + 과제 6) — 구현 완료, 검증 대기**: `story_manager.gd`에 friend 표시명 "친구"→"동현"(표정 5종 확장), 엑스트라 6종 등록(수학/담임 선생님·학생1~3·여자아이 — 미등록 ID는 이름표에 원시 ID가 노출되므로 사전 등록 필요), 수아 `shy`/`sad` 재매핑(둘 다 `sora_A101` 중복이었음 → 실물 확인 후 `A190`(은은한 미소)/`A102`(시무룩)), 로드 디렉터리에 `"test"` 추가. `choice_controller.gd`에 `condition` 평가 3줄 추가로 허브 이벤트 중복 선택 방지(H-1) 엔진 측 구현. **Godot 에디터가 실행 중이 아니라 godot-mcp 검증을 하지 못했다** — 다음 세션에서 stop→run 후 Parse Error·선택지 표시·이름표를 확인할 것.
- **허브 구조 해설 문서 작성 + 엔진 제약 7건 발굴**: `docs/06.hub-structure.md` 신설. 4월 허브(`AprilHub`)의 반복 구조·거리감 설계 의도·선형 파트와의 대비를 정리하고, **허브 이벤트 판정 기준 3조건**(순서 무관·가역성·완결성)을 세워 새 원고 프롤로그~3화가 전부 이를 위반함을 확인(C-5·C-6 분석 완료). 코드에서 확인한 함정 7건 기록 — `action:"jump"` 누락 시 진행 정지, 조건식 괄호 미지원, AND가 OR보다 먼저 split, 미정의 변수는 0으로 조용히 평가, 선택지 조건부 표시 엔진 미지원, `tracked_scenes` 하드코딩, `default` 누락 시 다음 줄 진행.
- **4월 허브 결정 7건 확정 (ADR-0004)**: C-5·C-6(자리배정·수학시간을 선형 도입부로 이관, 허브 이벤트 6종→4종), H-1(choice에 condition 필드로 중복 방지), H-2(시간 경과 페널티 **+10**, "보지 않았다" −15→−5), H-3(엔딩 A에 `distance <= 45`), H-5(허브 진입 시 구간별 내레이션), H-6(진행 조건 4→3회), H-4(화제 플래그, 원고 4~6화와 동시). **확정 중 발견**: C-5 이관으로 유일한 +15 선택지가 허브에서 빠져 페널티 +5로는 최악 플레이가 70에 그쳐 게임오버 재차 불가 → +10으로 재산출. 이에 맞춰 엔딩 A 임계값도 30→45(최선 플레이 도달치가 정확히 45). 검산: 최선 45(엔딩 A) / 보통 55(엔딩 B) / 최악 85(게임오버). `03.game-design.md` 3.1·3.3·4.1·4.2·4.4에 수치 반영, `05` C-5·C-6 해소 및 O-8(도입부 선택지 처리) 신설, Next-Tasks 과제 6(choice 조건부 표시 구현) 등록.
- **허브 문서를 설계/구현으로 분리 + 보완안 확정 대기**: `06.hub-structure.md`를 설계·밸런스 관점으로 재구성하고 기술 파트를 `07.hub-implementation.md`로 분리(연번 짝 규칙을 INDEX에 명문화). 허점 3종이 **연쇄**임을 확인 — H-1(중복 선택)을 고치면 H-2(게임오버 도달 불가)가, H-2를 고치면 H-3(4월 선택이 결말에 무영향)이 드러난다. **거리감 상승 선택지가 게임 전체에 3개(+25)뿐이라 최악 플레이도 70에 그쳐 게임오버 80에 못 미침**을 수치로 확인. 보완안: 허브 복귀마다 distance +5(시간 경과 페널티), "보지 않았다" −15→−5, 엔딩 A에 `distance <= 30` 조건, 허브 진입 시 상태 내레이션, 진행 조건 4→3회. H-1·H-2·H-5·H-6은 JSON 한두 줄 수준이라 **Phase 1 착수 전 확정 시 추가 작업 없음**.
- **허브 설계 결함 발견**: 이미 본 이벤트를 걸러내는 장치가 없어 **같은 이벤트를 4번 골라도 균열 파트로 진입**한다. `choice_controller.gd:45`가 조건 필터 없이 전 항목을 버튼화하기 때문. 해법 3안 비교 후 엔진 확장(choice에 condition 필드 추가) 권고.
- **JSON 변환 계획 수립**: Phase 0(엔진 정비) → 1~4(화 단위 변환) → 5(통합 검증) 5단계. 원고 3화까지 필요 배경이 전부 교실 계열이라 **에셋 블로커 없음**을 확인(반면 scene_map 등록 17개 중 10개는 파일 부재). 블로커 3건 식별 — 엑스트라 캐릭터 미등록 시 이름표에 원시 ID 노출, `story/test/`는 로드 디렉터리 하드코딩(`["april","may","crack","july"]`)에서 빠져 있어 과제 3 실행 시 테스트 노드 소실, 수아 표정 실그림 4종.
- **docs 번호 체계 재편 + archive 분리**: 새 원고 `01.new_story.md`를 기점으로 docs를 `00.INDEX`/`01.원고`/`02.캐릭터`/`03.기획`/`04.엔진스크립트`/`05.연속성`/`90.코드분석` 체계로 재편. 대체된 구버전 6종(캐릭터 시트 2·시놉시스·초안·확장본·선택지분기)은 `docs/archive/`로 이동하고 회수 대상을 README에 명시. `game_design_document.md`→`03.game-design.md`, `script_full.md`→`04.script-engine.md`, `main_scene_analysis.md`→`90.main_scene_analysis.md` 개명에 맞춰 `.claude/skills/update-docs.md` 경로 갱신.
- **원고 교체 방침 확정**: 새 원고와 기존 구조가 충돌하나 **게임 구조(허브형 + 거리감 스탯)는 유지하고 원고만 교체**하기로 결정. 04.script-engine.md는 구원고 기준이라 재작성 대기 상태로 표시하고, Next-Tasks에 과제 5(새 원고 반영)를 과제 1의 선행으로 신설.
- **설정 충돌 10건·미결 7건 목록화**: `docs/05.continuity.md` 신설. 현우의 과목 성향 반전(수학 못함/국어 잘함), 동현 신규 조연 등장, 소설 제목 공개 등은 새 원고 채택으로 해소하고 `02.characters.md`에 반영. 자리배정·수학시간의 허브 편입 가능 여부(C-5·C-6)와 동현 캐릭터 ID(C-8)는 미결로 남김.
- **OpenQuestions 2건 해소**: 프롤로그 정본 = `01.new_story.md`로 확정. 수아 표정은 JSON 실사용 기준 **6종**(sad 포함)으로 확정하고 `03.game-design.md`의 5종 표기를 오류로 기록.

## 2026-07-16
- **위키 온보딩 리뷰·정정**: 코드 대조 리뷰 후 Next-Tasks 과제 3 Test 노드 수 정정(25→24, 남길 실 노드 7개 명시), Context에 스토리 진행 구조(4월 허브→균열 파트→엔딩) 추가로 "균열 파트/게임오버" 용어 정의, 화면 흐름↔스토리 흐름 구분, index에 읽기 순서 힌트(Context→Next-Tasks) 추가.
- **위키 초기 작성**: Context 보강(게임 제목·거리감 메커니즘·기획 정본 위치), 열린 과제 4건 등록(스토리 JSON 변환 / Supabase 연동 완성 / Test 노드 분리 / 조건 분기 검증), 미결정 질문 5건 등록(plan §12 이관), 소급 ADR 3건 작성(JSON 노드 그래프 / 오버레이 모달 / 컨트롤러 분리).
- **하네스 설치**: LLM-WIKI 하네스 보일러플레이트로 프로젝트 위키 초기화.
